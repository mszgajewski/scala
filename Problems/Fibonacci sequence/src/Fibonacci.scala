object Fibonacci extends App {
  val a = 0
  val b = 1
  def sum(a: Int, b: Int): Int = 1

  println(a)
  println(a)
  println(b)
  println(sum(a, b))
  println(sum(println(sum(a, b))))
  println(sum(sum(), b)
}